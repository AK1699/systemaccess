# Path to the root folder the server is allowed to work inside.
# The server will ONLY touch repos/files inside this directory.
ROOT=/Users/akash/Desktop

# Port the server listens on (default 3000)
PORT=3000
