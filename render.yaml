services:
  - type: web
    name: systemaccess-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ROOT
        value: ./
